FIRST SETUP / RESET:
0. Open terminal: "pip install -r requirements.txt"
1. Run data_deletion.sql
2. Run create_db.py
3. Run data_creation, if you want test data
    To reach the test data in the webapp:
        Username: Test
        Password: Test

START THE WEBAPP:
1. Run __init__.py
2. Open http://127.0.0.1:5000 in your browser